using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class TractionSpot : MonoBehaviour
{
	
	public Mesh mesh;
	public float traction; //0-1 multiplier for wheel traction
	public float stuckFactor; //0-1 range for the chance of disabling all traction on a rover
	
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
